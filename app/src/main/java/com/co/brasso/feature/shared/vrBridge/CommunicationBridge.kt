package com.co.brasso.feature.shared.vrBridge

import android.util.Log


class CommunicationBridge {

    fun closePlayer() {
        Log.d(
            "UnityCalled",
            "Callback with two parameters: "
        )
       // VLCPlayerActivity.instance?.finishActivity()
    }
}